\begin{code}
module TcUnify where
import TcType	( TcTauType )
import TcRnTypes( TcM )
import Coercion (Coercion, Mk)

-- This boot file exists only to tie the knot between
--		TcUnify and Inst

unifyType :: TcTauType -> TcTauType -> TcM (Mk Coercion)
\end{code}
